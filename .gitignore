plans/
.DS_Store
node_modules/
dist/
coverage/
storybook-static/
*.log
.env
.env.*
!.env.example

# Local-only Storybook scratchpad — dev stories stay ignored.
# The committed Storybook config (.storybook/) and the MarchMadness demo
# (stories/) are tracked; local experiments live in dev-stories/.
dev-stories/
